<div dir="rtl" style="text-align: right;">

# سكانر أوردرات المرتجعات (`Bosta-Orders-Returned-Scanner`)

![version](https://img.shields.io/badge/version-v2.0.0-blue)

**بتعمل إيه:** الموظف بيمسح تراكينج نمبر بوسطة لأوردرات راجعة (RTO أو مرتجع بعد
التسليم)، والأداة بتتحقق من الحالة على شوبيفاي وتحدّث ميتافيلد S1/S2 + تلغي
الأوردر (مسار RTO) أو تسترجع القطع للمخزون (مسار مرتجع بعد التسليم).
**مين بيستخدمها:** مخزن (استلام المرتجعات).
**الإصدار:** Worker `v3.4.0` · الواجهة `v3.4`

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Bosta-Orders-Returned-Scanner/
الـ Worker : https://bosta-orders-returned-scanner.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: bosta-orders-returned-scanner   ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `check_employee` / `register_pin` / `verify_employee` / `log_logout` / `get_employees` | تسجيل الدخول (D1 Auth القياسي) |
| `get_config` | نسخة الـ Worker + وقت السيرفر — لمطابقة نسخة الواجهة |
| `diag` | فحص ذاتي كامل (env · CORS · D1 · شوبيفاي + الصلاحيات · LOCATION_ID · بوسطة) — صفر كتابة |
| `lookup` | بحث بوسطة بالتراكينج + فحص شوبيفاي (S1/S2/returnStatus) + التحقق من صلاحية الانتقال — بدون كتابة |
| `update` | تنفيذ فعلي: إلغاء الأوردر (RTO) أو استرجاع مخزون (مرتجع بعد التسليم) + تحقق بعد التنفيذ + D1 log · وبياخد كمان `rejected[]` فبيسجّلها بـ `type = 'rejected'` **من غير أي تنفيذ** (v3.4.0) |
| `get_logs` / `get_logs_count` | تاب السجل — فلترة server-side بقوايم (`employees` · `results` · `machines`) + `search` + `dateFrom`/`dateTo` + **ترتيب server-side** (`sortBy`/`sortDir` بقائمة بيضاء)، صفحة ١٠٠ صف |
| `get_logs_export` | التصدير — بيرجّع `{ entries, cap, total, truncated }`، مش الصفوف لوحدها |

## D1

```
tool  : metafields_change   ← (extra.sourceTool = "bosta_orders_returned_scanner")
type  : update              ← الصفوف اللي **لمست شوبيفاي فعلاً** (نجحت أو فشلت أثناء التنفيذ)
                               extra.result = 'success' | 'warning' | 'error'
type  : rejected            ← v3.4.0 — الصف اللي **اتوقف قبل أي تنفيذ**
                               extra.result = 'rejected' | 'already'
                               extra.stage  = 'lookup'   | 'write'
tool  : bosta_return        ← login/logout بس (تاريخي: كان فيه type=returned قبل v3.0.0)
type  : login · logout
```

جداول إضافية: لا شيء — `logs` و`employees` القياسيين بس.

> 🔴 **`type = 'rejected'` لسه مش مسجّل في `ecommoda-constants` §7.** القيمة
> دي بتتكتب من `bosta-orders-shipped-scanner` من v3.3.0، و**بقت بتتكتب من
> هنا كمان** من v3.4.0. القاعدة هناك: أي قيمة تتسجّل **قبل** أول `writeLog`.
> التسجيل **مطلوب من أحمد** (صف "Metafields Change Log" → `update` · `rejected`).

> ⚠️ **الفصل بين النوعين مقصود ولازم يفضل.** قبل v3.4.0 كان **كل** صف بيتكتب
> تحت `update` — بما فيهم ٦١ صف فشل ما لمسوش شوبيفاي أصلاً. يعني استعلام خط
> الأساس (اللي بيعدّ `update`) كان بيقيس **المحاولات** مش الكتابة الفعلية.

> ⚠️ **`extra.stage` بيفرّق حالتين مختلفتين تمامًا:** `lookup` = اترفض وقت
> الاستعلام (الموظف شافه قبل ما يضغط) · `write` = **كان متوافق واتوقف وقت
> التنفيذ**، يعني سباق: موظف تاني حرّك الأوردر في الوقت الفاصل.

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET · BOSTA_API_KEY
Vars     : SHOP_DOMAIN · LOCATION_ID   ← من [vars] في wrangler.toml
Build watch paths : index.js · wrangler.toml   ← chip منفصل لكل واحد (اتضيّقت 05-09-2026)
```

⚠️ **صلاحيات Shopify المطلوبة على التطبيق (WareHouse-App):** `read_orders` ·
`write_orders` · `read_returns` · `write_returns` · `read_inventory` ·
`write_inventory` · `read_locations`. غياب `write_returns` هو السبب اللي أنتج
v3.2.0 (راجع "فخاخ الأداة دي" تحت). شغّل `?action=diag` بعد أي تعديل على
صلاحيات التطبيق للتأكد. **السبعة كلهم بيتفحصوا في `REQUIRED_SCOPES`** من
v3.3.0 (كانوا أربعة بس، و`read_locations` بالذات فحص الـ LOCATION_ID معتمد
عليها).

## CORS

`ALLOWED_ORIGINS` صارمة (بدون wildcard) — الأداة دي بتلغي أوردرات وبتحرّك
مخزون، أداة كتابة مش قراءة.

## خط الأساس بعد النقل — ✅ **اتقفل 07-09-2026**

> من D1 (الأداة كانت شغّالة من الداشبورد قبل النقل — راجع §0-ب في السكيل):
> **123 صف** مسجَّل (`tool='metafields_change' AND type='update' AND
> extra LIKE '%"sourceTool":"bosta_orders_returned_scanner"%'`)، آخر واحد
> `2026-08-26T12:51:35.950Z`.

> ✅ **القياس الفعلي 07-09-2026: 615 صف**، آخر واحد `2026-09-07T11:59:01Z`.
> الأداة بتكتب والعدّاد بيزيد — البند مقفول.

> 🔴 **تصحيح مهم في الاستعلام نفسه.** الرقم ٦١٥ **مش** ٦١٥ نجاح: هو
> `552 success + 61 error + 2 warning`. يعني الاستعلام القديم كان بيقيس
> **المحاولات** مش الكتابة الفعلية. الاستعلام الصح:

```sql
SELECT COUNT(*) as total, MAX(timestamp) as last_ts FROM logs WHERE tool = 'metafields_change' AND type = 'update' AND json_extract(extra,'$.result') = 'success' AND extra LIKE '%"sourceTool":"bosta_orders_returned_scanner"%';
```

> ومن v3.4.0 المشكلة اتقفلت من المصدر: الصفوف اللي ما لمستش شوبيفاي بقت تحت
> `type = 'rejected'` مش `update`.

## 🔴 الاكتشاف الحاكم في مراجعة 07-09-2026 — والإصلاح

**٥٩ من ٦١ صف فشل في السجل (٩٧٪) كانوا على أوردرات الأداة نجحت فيها قبل كده.**
٢٦ منهم خلال أقل من دقيقتين من النجاح. أوضح مثال — أوردر `#53539` يوم 07-09:

```
11:58:52.175Z  →  success  (S1 → Returned · الأوردر اتلغى · المخزون رجع)
11:59:00.359Z  →  error    "الحالة تغيرت قبل التحديث — S1 الحالي: Returned"
                  فرق ٨ ثواني بالظبط
```

**السبب:** بعد دفعة ناجحة بالكامل كانت الصفوف بتفضل `selected`، والزرار بيفضل
مكتوب عليه نفس العدد، ومفيش نافذة ملخّص، والتوست بيختفي في ٣ ثواني — يعني
**الشاشة بعد النجاح شكلها بالظبط زي الشاشة قبل الضغط**. الموظف في مخزن بيبص
بعيد وبيرجع، فبيضغط تاني.

**الأثر:** ١٠٪ من سجل دايم بقى صفوف حمرا كذّابة، وأي تقرير عن "نسبة فشل
الأداة" غلط بالكامل — والأهم إن الموظف اتعلّم إن الأحمر طبيعي. ده نفس المرض
اللي `verifyCancels` v3.3.0 اتكتبت عشانه («الحالة الاستثنائية بقت الافتراضية»)
بس على الأحمر المرة دي.

**⚠️ والحماية الوحيدة اللي منعت كارثة كانت `validateTransition`** — لو حد وسّع
القاعدة يومًا ما ليقبل `Returned` كحالة بداية، الضغطة التانية كانت هتلغي
أوردرات تاني. الحماية كانت **بالصدفة مش بالتصميم**.

**الإصلاح (v3.4.0 / الواجهة v3.4) — أربع طبقات، كل واحدة لوحدها بتقفل المشكلة:**

1. **نزع سلاح الصف** بعد تنفيذه (`doneInSession`) — بيتشال من الاختيار
   وبيتقفل، والزرار بيرجع رمادي.
2. **نافذة ملخّص إجبارية** بتقف قدام الشاشة لحد ما تتقفل.
3. **حالة `already` مستقلة** — "خلاص اترجّع" بقت لون محايد وعدّاد مستقل،
   مش `error` أحمر.
4. **نافذة تأكيد** قبل التنفيذ بتسمّي الأثر (كام إلغاء نهائي · كام استرجاع).

> ⚠️ **أي جلسة جاية بتلمس `startUpdate`: نزع السلاح مش تفصيلة تجميلية.**
> رجوع الصفوف للاختيار بعد النجاح بيرجّع الباج ده بالكامل.

## فخاخ الأداة دي

- **`reverseFulfillmentOrderDispose`/`orderCancel` وأخواتهم بيرجّعوا نجاح شكلي
  حتى لو شوبيفاي رفضت العملية فعليًا** (خطأ GraphQL علوي مع `data: null` —
  الـ `userErrors` بترجع `[]` في الحالة دي). ده اللي سبب عطل 19→23-08-2026:
  الأداة سجّلت `reverseDispose` على ٧ أوردرات وكلها كذب لأن التطبيق كان ناقصه
  `write_returns`. `shopifyGQL` (v3.2.0) بتتحقق من HTTP status + GraphQL
  errors علوية قبل ما تعتبر أي رد نجاح — **متلمسش الدالة دي من غير قراءة
  التعليق فوقها كامل**.
- **`orderCancel` بترجّع `job` مش نتيجة نهائية** — الإلغاء بيحصل بعد الرد
  بثانية أو اتنين. التأكيد الحقيقي في `verifyCancels()` بعد الدفعة، مش من رد
  `orderCancel` نفسه.
- **`RETURN_REQUESTED` مش كافي لمسار المرتجع بعد التسليم** (قرار Ahmed
  23-08-2026) — المرتجع لسه محتاج approve من CS الأول، وإلا شوبيفاي لسه ما
  أنشأتش `reverseFulfillmentOrder` والمخزون مش هيرجع حتى لو الميتافيلد اتكتب.
- **`LOCATION_ID` غلط أو ناقص بيتحول لـ GID فاسد** لو ما فيش حراسة —
  `requireLocationId()` بترمي خطأ باسم المتغير صراحة بدل الفشل الصامت.
- **قراءة التحقق بعد `orderCancel` لازم تستنى الـ Job، مش تقرا فورًا** (v3.3.0).
  القراءة الفورية بتفشل دايمًا تقريبًا (الميوتيشن بتتقبل وشوبيفاي بتسجّل
  `cancelledAt` بعدها بثانية)، فكل إلغاء ناجح كان بيطلع **أصفر "مش مؤكَّد"** —
  والاستثناء لما يبقى هو الافتراضي، الموظف بيبطّل يقرا الأصفر. `verifyCancels`
  دلوقتي بتعمل backoff متصاعد + بتفحص `job(id){ done }` نفسه + بتقف على أول
  تأكيد، وبترجّع `{ jobDone, attempts, waitedMs }`. **متحوّلهاش لنوم ثابت.**
- **رقمين تتبع ممكن يرجعوا لنفس الأوردر** (شحنة RTO + شحنة مرتجع). `freshMap`
  بتتقري **مرة واحدة قبل حلقة التنفيذ**، فمن غير حارس التكرار (`seenOrders`)
  الأوردر كان بياخد `orderCancel` مرتين على نفس اللقطة القديمة — والإلغاء لا
  رجعة فيه.
- **الأداة بتقبل أي `orderType` من بوسطة غير `Send`** — قرار مقصود (أحمد
  05-09-2026). حراسات S1/S2/`returnStatus` هي اللي بترفض اللي مالوش مسار
  صحيح؛ قايمة سماح مقفولة كانت هترفض شحنات سليمة أول ما بوسطة تضيف نوع جديد.
- **الواجهة بتطابق نتيجة كل صف بالـ Worker بالترتيب مش بالاسم** — الـ Worker
  بيرجّع نتيجة **لكل عنصر في `items` بنفس الترتيب**، ورقمين تتبع ممكن يرجعوا
  لنفس الأوردر. ⚠️ **أداة الشحن عكسها بالظبط** (بتطابق باسم الأوردر، لأن
  الـ Worker هناك بيرسم الكتابة الناجحة في حلقة تانية) — **متنقلش المنطق بين
  الأداتين**.
- **إعادة الاستعلام بتستبدل الصف، مش بتتخطّاه** (v3.4). قبل كده كان الرقم
  الموجود في الجدول بياخد رد جديد من الـ Worker **ويترمى** — الزرار يبان إنه
  استعلم وهو بيعرض حالة اتقرأت من ٢٠ دقيقة. أي تعديل جاي على `mergeLookupResults`
  يفضل محافظ على القاعدة: **الصف المعروض = آخر استعلام فعلي**.
- **نتيجة كتابة سابقة وعلامة `doneInSession` بتتحافظ عليهم عبر إعادة الاستعلام**
  — دول سجل لفعل حصل، مش حالة بتتقرا من شوبيفاي. اللي بيتحدّث هو
  `s1`/`s2`/`returnStatus`/`valid` بس.
- **التنفيذ بيتقسّم على مجموعات ١٠ في الواجهة** (مش ١٥ زي أداة الشحن) — كل
  أوردر هنا بياخد نداءين على الأقل زائد حلقة تحقق الإلغاء. مش تحسين شكلي:
  نداء واحد لـ ٦٣ أوردر ≈ ١٣٠ نداء شوبيفاي متتابع ≈ ٤٠-٦٠ ثانية والشاشة
  ساكتة، والسكوت ده هو الوقود المباشر للضغطة التانية. **متحوّلهاش لنداء واحد.**
- **`fetchOrderReturns` بتقص عند 20/20/100 بدون pagination** — القصّ بقى
  **بيتكشف** من v3.4.0 (`nodes.truncated`) وبيطلّع تحذير على الصف بدل ما
  يعدّي في صمت. الحالة نظرية عمليًا (أوردر بأكتر من ٢٠ مرتجع)، بس الاسترجاع
  الجزئي اللي بيتقال عنه "تم" هو نفس عيلة الفشل اللي الأداة كلها متكتبة عشانها.
- **رسايل شوبيفاي المتكررة بقت بالعربي جنب النص الأصلي** (`humanizeShopifyError`).
  اتنين منهم ظهروا فعلاً في السجل: `Cannot cancel an order that has outstanding
  fulfillments` و`already been canceled`. ⚠️ **النص الإنجليزي مايتشالش** — هو
  اللي بيتبحث بيه وقت التشخيص.
- ⚠️ **`orderCancel(reason: 'CUSTOMER')`** — أي تقرير مستقبلي عن أسباب الإلغاء
  هيقول "العميل طلب" على شحنات العميل ما استلمهاش أصلاً. القيمة **ما اتغيّرتش**
  عن قصد: `DECLINED` في شوبيفاي معناها "الدفع اترفض" (مش دي الحالة)، و`OTHER`
  بتضيّع المعلومة كلها. البند مسجّل هنا كملاحظة على التقارير مش كباج.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخ المرقّمة القديمة (2.0.html · 3.0.html · 3.1.1.html · Index.html القديمة)
محفوظة في commit: eed022f1f8bb2a654d8a6b0dd2a9532c7cc27dc0
git show eed022f1f8bb2a654d8a6b0dd2a9532c7cc27dc0:2.0.html
```

## بصمة المهارات

> الصيغة والقواعد والمهارات اللي بتدخل الجدول → `ecommoda-skill-versioning`
> Step 4. مهارة مالهاش رقم إصدار مابتدخلش الجدول.

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v2.1.0 |
| ecommoda-html-builder | v6.6.0 |
| ecommoda-constants | v1.10.0 |
| ecommoda-order-lifecycle | v1.2.0 |
| shopify-graphql-helper | v1.0.0 |

آخر مطابقة: 07-09-2026 · `index.js` v3.4.0 · `index.html` v3.4
🔴 معلّقة: — لا شيء

> `shopify-graphql-helper` و`ecommoda-order-lifecycle` اتضافوا للجدول 05-09-2026:
> الاتنين دخلوا نظام الإصدارات (قسم أ في `references/skill-registry.md`) وشكّلوا
> الكود فعلًا — عقد `shopifyGQL` وتأكيد الميوتيشن من الأولى، ونصوص الحالات
> وجدول الانتقالات من التانية. `bosta-api-helper` **مش** في الجدول رغم إنها
> شكّلت الكود كمان — لسه في القسم (ب) ومالهاش رقم إصدار، ومينفعش نختم برقم
> مش موجود.

## مسائل مفتوحة

- 🔴 **تسجيل `type = 'rejected'` في `ecommoda-constants` §7 — مطلوب من أحمد.**
  القيمة بتتكتب من `bosta-orders-shipped-scanner` من v3.3.0، و**من هنا كمان**
  من v3.4.0 — يعني كاتبين مش واحد. الصف المطلوب: "Metafields Change Log" →
  `update` · `rejected` · `login` · `logout`. من غير التسجيل ده، ده تكرار
  لنفس مخالفة `shopify_woo_sync` (صفوف اتكتبت قبل التسجيل) — ومرتين بدل مرة.
- 🟡 **`read_all_orders` مش من صلاحيات التطبيق.** فحص `diag` بقى بيقولها من
  v3.4.0. غيابها **مش عطل حالي** (الأوردرات المستخدمة أحدث من ٣٠ يوم)، لكن
  شوبيفاي بتخفي أي أوردر أقدم من **٦٠ يوم** عن تطبيق مالوش الصلاحية —
  **والأداة دي أكتر واحدة معرّضة**: مرتجع بيوصل بعد شهرين هيطلّع "الأوردر غير
  موجود على شوبيفاي" وهي رسالة كاذبة تمامًا. القرار لأحمد.
- 🟡 **`dispositionType` مثبّت على `RESTOCKED`** — كل قطعة راجعة بترجع مخزون
  **صالح للبيع**، ومفيش مسار للقطعة التالفة. شوبيفاي بتدعم
  `DAMAGED`/`DISPOSED`/`PROCESSING_REQUIRED`. **قرار تشغيلي لأحمد** (بيحتاج
  خطوة زيادة على الموظف في المخزن) — مش باج، بس الأثر حقيقي: قطعة تالفة
  بترجع للبيع.
- 🟡 **PIN بيتخزّن plaintext ومفيش حد لمحاولات الدخول** (٤ أرقام = ١٠ آلاف
  احتمال). الكود جوّه بلوك `§SHARED` المكتوب فوقه "انسخه حرفيًا" — **الإصلاح
  مكانه `ecommoda-worker-builder` مش الأداة دي**.
- 🟡 **`WORKER_SECRET` عند العميل = نسبة العمليات للموظف مش دليل.** أي موظف
  يقدر ينادي `?action=update` مباشرةً ويكتب أي `employee` في السجل. **في
  الأداة دي تحديدًا ده أخطر** (الفعل إلغاء لا رجعة فيه). قيد معماري على مستوى
  الستاك (توكن لكل موظف).
- الأداة اتنقلت من دفعة واحدة (كانت شغّالة من الداشبورد قبل النقل) — الربط في
  Cloudflare (Settings → Build → Connect) والأسرار الأربعة (§Auth) لسه مطلوبين
  من أحمد يدويًا. لحد ما يحصل الربط، الـ Worker المنشور فعليًا مصدره **الرفع
  اليدوي القديم من الداشبورد**، مش الريبو ده.
- ✅ **Build watch paths اتضيّقت** (05-09-2026 — أكّد أحمد إنها اتعملت):
  `index.js` + `wrangler.toml`، كل واحد **chip منفصل**. يعني تعديل `index.html`
  أو `.md` لوحده مابقاش بينشر الـ Worker.
  ⚠️ **الفخ المصاحب:** لو الأداة كبرت وضافت ملف جديد بيعتمد عليه الـ Worker
  (`package.json`، فولدر `src/`، ملف config) — **لازم يتضاف للـ paths**، وإلا
  الـ Worker هيفضل على نسخة قديمة **من غير أي رسالة**.
- 🟡 **سبب تغيير S1 — مؤجَّل بقرار (أحمد 05-09-2026).**
  `ecommoda-order-lifecycle` §1.5 بيقول إن أي Worker بيكتب `manual_status` لازم
  **يطلب سبب** ويرفض `400` من غيره (S1 بس). الأداة دي بتكتب
  `manual_status = Returned` من غير سبب، و**ده وضع مقبول مؤقتًا بقرار صريح** —
  مش سهو ومش بند منسي.
  **ليه السؤال قايم أصلًا:** نص المهارة بيقول "لحد دلوقتي **Order Status
  Updater** هي الوحيدة اللي بتكتب S1 **يدويًا**"، والقاعدة متكتبة على أداة
  الموظف بيختار فيها الحالة من قايمة. هنا الأداة بتستنتج الحالة (قيمة واحدة
  ممكنة: `Returned`) والسبب ضمني — الشحنة رجعت للمخزن والسكان نفسه هو الإثبات.
  **الخيارات وقت ما يتحسم:** (أ) تضييق نطاق القاعدة في المهارة لتبقى "بكتابة
  S1 **باختيار بشري**" — صفر تعديل كود · (ب) سبب ثابت تلقائي
  (`extra.reason` + `extra.reasonSource = 'auto:scanner'`) من غير ما يتسأل
  الموظف · (ج) تنفيذ حرفي بقايمة أسباب ورفض `400`.
  ⚠️ **الأثر المعروف والمقبول:** أي تقرير مستقبلي عن "أسباب الإرجاع" هيبقى
  ناقص الأداة دي بالكامل لحد ما البند ده يتقفل.

---

آخر تحديث: 05-09-2026 — مراجعة كاملة مقابل `ecommoda-worker-builder` v2.0.0
و`ecommoda-html-builder` v6.3.0: قفل البندين الكاسرين (انتظار الـ Job المشروط ·
عقد `get_logs_export`) على الطرفين، وتحديث بصمة المهارات. ومعاها قفل بند
watch paths، وتثبيت قرار تأجيل سبب تغيير S1 كبند مفتوح معروف.

نفس اليوم — v3.3.1: إصلاح جدول السجل وجدول النتائج (`index.html`) كانا
بيفتحوا على آخر أعمدتهم بدل أولها بسبب تعارض `dir="rtl"` على الصفحة مع
`direction: ltr` على الجدولين من غير ما يتحدد على حاويتي السكرول
(`.table-wrap`/`.log-table-wrap`). مراجعة كاملة للواجهة مقابل
`ecommoda-html-builder` v6.4.0 (البند الجديد الوحيد فيها، `.btn-red`، مش
منطبق — الأداة مفيهاش نافذة بحالة سلبية/نهائية) — الباقي متوافق ومفيش تغيير
تاني مطلوب، فبصمة `ecommoda-html-builder` فضلت v6.3.0 (لسه هي اللي شكّلت
الكود فعليًا).

نفس اليوم — v3.3.2: إصلاح v3.3.1 صحّح اتجاه السكرول بس ما كانش كافي — عمود
"التحديث" في السجل كان بيحط سبب الفشل الكامل (`e.notes`) في `.tracking-num`
(نص قصير سطر واحد، بترث `white-space: nowrap`) بدل `.reject-reason`
(بتلف + `max-width: 230px`) المستخدمة أصلًا لنفس الغرض في جدول النتائج. أي
صف برسالة رفض طويلة كان بيمدّد الجدول كله لعرض أكبر بكتير من الشاشة (اتقاس:
٢٣٣٤px بدل ٩٠٠px)، فالموظف كان لسه محتاج يمرّر مسافة كبيرة — الاتجاه بس هو
اللي اتقلب في v3.3.1، مش المشكلة الأصلية. **درس:** لما عمود واحد بيتحكم في
عرض الجدول كله، افحص أطول محتوى ممكن يظهر فيه فعليًا (رسالة رفض/خطأ كاملة)
قبل ما تقول إن إصلاح السكرول كفاية.

## بعد نشر v3.4.0 / الواجهة v3.4 — تأكيدات مطلوبة

> ⚠️ **الواجهة بتطلب Worker ≥ v3.4.0** (`MIN_WORKER_VERSION`). لو الـ Promote
> ما تمّش، بادج «⚠️ الـ Worker نسخة قديمة» هيظهر — **مش تحذير تجميلي**:
> الواجهة بتبعت `rejected[]` و`sortBy` وبتقرا حالة `already`، والـ Worker
> القديم بيتجاهلهم في صمت.

1. **Promote** بعد أول build، وبعدها 🩺 في الإعدادات — لازم كل الفحوصات ✅
   (خصوصًا `write_returns` و`LOCATION_ID`). الفحص الجديد **`read_all_orders`**
   ممكن يطلع ❌ وده **مقبول دلوقتي** (راجع المسائل المفتوحة).
2. **اختبار نافذة التأكيد (أهم واحد):** اختار أوردر RTO واحد واضغط "تحديث
   شوبيفاي" — لازم تظهر نافذة حمرا مكتوب فيها **"🚫 ١ أوردر هيتلغي نهائيًا"**
   قبل أي تنفيذ، والضغط على "إلغاء" مايعملش أي حاجة على شوبيفاي.
3. **اختبار نزع السلاح:** بعد ما التنفيذ يخلص اتأكد إن: الـ checkbox بقى
   مقفول · الزرار رجع رمادي · نافذة الملخّص فتحت · عمود "التحديث المطلوب" بقى
   "⏭ خلاص اتعمل". **لو الزرار فضل شغّال بنفس العدد، الإصلاح الأساسي مش شغّال.**
4. **اختبار إعادة الاستعلام الحقيقية:** استعلم عن رقم، غيّر حالته على شوبيفاي
   من تاب تاني، اضغط "استعلام بوسطة" تاني — **لازم الصف يعكس الحالة الجديدة**.
   لو فضل زي ما هو، `mergeLookupResults` مش شغّالة.
5. **اختبار "خلاص اترجّع":** استعلم عن أوردر حالته `Returned` أصلاً — لازم
   يبان **رمادي** بجملة "المرتجع ده اتقفل خلاص"، **مش أحمر** وبرسالة بطول
   ٢٠٠ حرف.
6. **اختبار السكانر:** اسكن باركودين ورا بعض بسرعة — لازم يدخلوا رقمين
   منفصلين، ولو اتلزقوا تظهر رسالة "طوله N رقم".
7. **الـ CORS:** افتح الأداة من رابط GitHub Pages وتأكد إنها شغّالة.

## استعلامات المتابعة بعد النشر

```sql
SELECT type, json_extract(extra,'$.result') AS res, COUNT(*) n, MAX(timestamp) last_ts FROM logs WHERE tool='metafields_change' AND extra LIKE '%"sourceTool":"bosta_orders_returned_scanner"%' AND timestamp > '2026-09-07' GROUP BY type, res;
```

> **المتوقّع بعد النشر — وده اختبار الإصلاح نفسه:** `update`/`error` لازم
> **يقرب من صفر** (قبل الإصلاح كان ٦١ صف، ٥٩ منهم كذّابين). أي `error` جديد
> تحت `update` معناه **فشل حقيقي أثناء التنفيذ** ويستاهل تحقيق فوري.
> و`rejected`/`already` بيظهر — ده صحّي، دي الحالات اللي كانت بتتسجّل فشل.

آخر تحديث: 07-09-2026 — 18:30


</div>
